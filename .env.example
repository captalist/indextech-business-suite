# Copy to .env and adjust before production deploy
POSTGRES_PASSWORD=suite_secret_change_me
JWT_SECRET=change-this-secret-in-production
ADMIN_PASSWORD=admin123

# Local dev (PostgreSQL running on localhost):
# DATABASE_URL=postgresql://suite:suite@localhost:5432/indextech_suite
